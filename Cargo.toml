[workspace]
resolver = "2"
members = ["crates/*"]

# Workspace-wide build profile defaults. Individual member packages can still
# override these on a per-package basis if they want different opt levels.
[profile.release]
opt-level = "s"

[profile.dev]
debug = true
opt-level = "z"
